PORT=3001
DATABASE_URL=postgresql://postgres:password@localhost:5432/bundl
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Tranzila (mock in V1)
TRANZILA_TERMINAL=your_terminal
TRANZILA_PASSWORD=your_password

# Nodemailer (mock in V1 - logs to console)
EMAIL_FROM=noreply@bundl.app
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
